import SettingsContent from "@/components/admin/SettingsContent";

export const metadata = { title: "Settings | MedClinical" };

export default function SettingsPage() {
  return <SettingsContent />;
}
