Use App Router.

Server Components first.

Client Components only when required.

Use Server Actions.

No API routes unless necessary.

Business logic inside lib/.

No duplicated queries.

No duplicated types.

Use aliases.

